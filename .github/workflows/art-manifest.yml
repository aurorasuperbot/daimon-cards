name: Art Manifest Release

# Builds the lazy-art assets — a small `manifest.json` + per-card
# tarballs — and uploads them as GitHub Release assets so the runtime
# manifest fetcher (``daimon.update.manifest.fetch_manifest``) can
# resolve them.
#
# Triggered by `art-v*` tags pushed to THIS repo (daimon-cards).
# The release tag becomes the ``pack_version`` that ships in the
# manifest payload, so a card pack upgrade is just "build new manifest +
# per-card tarballs, push tag".
#
# REPLACES the legacy monolithic ``art-pack-v*`` flow: instead of a
# single 1.6GB tarball that every install pulls down on first launch,
# we ship one ~5–13MB tarball per card plus a manifest small enough
# (~50KB) to fetch on every onboarding run.
#
# Pure Python build using ``scripts/build_art_manifest.py`` — stdlib
# only, no ``pip install`` needed (the manifest schema is vendored into
# the script). When ``daimon-engine`` lands on PyPI we can collapse
# the vendored schema and ``pip install daimon-engine`` here.

on:
  push:
    tags:
      - "art-v*"
  workflow_dispatch:
    inputs:
      pack_name:
        description: "Logical pack name written into the manifest"
        required: false
        default: "v1_alpha"
      art_source_dir:
        description: "Path inside the repo to scan for card art (one subdir per card_id)"
        required: false
        default: "art/v1_alpha"
      starter_card_ids:
        description: "Comma-separated card_ids to mark as starter pack (prefetched at onboard). Empty = first 10 alphabetical."
        required: false
        default: ""

permissions:
  contents: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build art manifest + per-card tarballs
        env:
          PACK_VERSION: ${{ github.ref_name }}
          PACK_NAME: ${{ github.event.inputs.pack_name || 'v1_alpha' }}
          # The art source dir contains one subdir per card_id with
          # base.png + manifest.json + variants/*.png. For V1 this is
          # checked into the repo at art/v1_alpha/.
          ART_SOURCE_DIR: ${{ github.event.inputs.art_source_dir || 'art/v1_alpha' }}
          # Where the runtime will fetch per-card tarballs from.
          # The release asset URL pattern is stable.
          ASSET_BASE_URL: "https://github.com/${{ github.repository }}/releases/download/${{ github.ref_name }}"
          STARTER_IDS: ${{ github.event.inputs.starter_card_ids || '' }}
        run: |
          set -euo pipefail
          mkdir -p dist
          if [[ ! -d "${ART_SOURCE_DIR}" ]]; then
            echo "::error::Art source directory '${ART_SOURCE_DIR}' not present in checkout."
            echo "::error::Either commit the art under that path or pass a different"
            echo "::error::art_source_dir via workflow_dispatch."
            exit 1
          fi
          STARTER_FLAG=""
          if [[ -n "${STARTER_IDS}" ]]; then
            STARTER_FLAG="--starter-card-ids ${STARTER_IDS}"
          fi
          python scripts/build_art_manifest.py \
            --input "${ART_SOURCE_DIR}" \
            --output dist \
            --pack-name "${PACK_NAME}" \
            --pack-version "${PACK_VERSION}" \
            --asset-base-url "${ASSET_BASE_URL}" \
            ${STARTER_FLAG}

      - name: Summarize release assets
        run: |
          echo "::group::dist/ contents"
          ls -la dist/
          echo "::endgroup::"
          echo "Manifest:"
          head -20 dist/manifest.json
          echo "..."
          echo "Total assets: $(ls dist | wc -l)"
          echo "Total size:   $(du -sh dist | cut -f1)"

      - name: Upload run artifacts
        uses: actions/upload-artifact@v4
        with:
          name: art-manifest-${{ github.ref_name || github.run_id }}
          path: dist/
          if-no-files-found: error
          retention-days: 14

      - name: Create / update GitHub Release
        if: startsWith(github.ref, 'refs/tags/art-v')
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: "DAIMON Art Pack ${{ github.ref_name }}"
          body: |
            Lazy-art manifest + per-card tarballs.

            * `manifest.json` — fetched on every onboarding run; small
              (~50KB) so the cost is negligible.
            * `card_<card_id>.tar.gz` — fetched JIT when a card first
              needs to render, cached locally afterwards.

            Verify each asset against the matching `.sha256` sidecar.
            The runtime fetcher does this automatically; if you mirror
            assets manually, run:

            ```
            sha256sum -c manifest.json.sha256
            ```

            Engine compatibility: requires `daimon-engine` with
            `COMPAT_ART_MAJOR == 1`.
          files: |
            dist/manifest.json
            dist/manifest.json.sha256
            dist/card_*.tar.gz
            dist/card_*.tar.gz.sha256
          fail_on_unmatched_files: true
          draft: false
          prerelease: false
